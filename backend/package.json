{
  "name": "pawlink-contact-backend",
  "version": "1.0.0",
  "description": "Contact form backend for the PawLink landing page — sends submissions via SMTP to the partnerships inbox.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "nodemailer": "^6.9.14"
  }
}
